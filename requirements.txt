# Runtime dependencies. Install with:  pip install -r requirements.txt
anthropic>=0.40
openai>=1.40
google-api-python-client>=2.100
google-auth>=2.30
google-auth-oauthlib>=1.2
msal>=1.28
requests>=2.31
feedparser>=6.0
python-dotenv>=1.0
tzdata>=2024.1
discord.py>=2.3
psycopg[binary]>=3.1
pgvector>=0.3

# Voice puck (local mic loop — jobs.converse; not needed on the Fly image):
# sounddevice>=0.4
# numpy>=1.26
# openwakeword>=0.6   # --wake hands-free trigger
# onnxruntime>=1.17   # openWakeWord inference backend
# webrtcvad>=2.0      # end-of-speech detection

# Dev only:
# pytest>=8.0
